---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-shortener
spec:
  template:
    spec:
      containers:
        - name: url-shortener
          command:
            - --environment
            - dev
